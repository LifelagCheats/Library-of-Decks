#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

cd frontend

# Lint only staged files (via lint-staged)
npx lint-staged

# Security audit (global check)
npm audit --audit-level=moderate

# Build dry-run + cleanup
npm run build
rm -rf dist build

echo "✅ All checks passed. Committing..."
